import {StyleSheet} from 'react-native';
import {FONT_BOLD, FONT_REGULAR, FONT_SEMIBOLD} from '../../constants';

export const styles = StyleSheet.create({
  h1: {
    fontSize: 22,
  },
  h2: {
    fontSize: 18,
  },
  p: {
    fontSize: 16,
  },
  bold: {
    fontFamily: FONT_BOLD,
  },
  regular: {
    fontFamily: FONT_REGULAR,
  },
  semibold: {
    fontFamily: FONT_SEMIBOLD,
  },
});
